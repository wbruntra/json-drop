# Point the @wbruntra scope at the GitHub Packages npm registry.
# The token is read from the GITHUB_TOKEN env var — never commit a real token.
@wbruntra:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
always-auth=true
