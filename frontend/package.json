{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build index.html --outdir dist --minify && cp -r public/* dist/",
    "dev": "bun build index.html --outdir dist --watch && cp -r public/* dist/"
  },
  "dependencies": {
    "preact": "^10.29.2"
  }
}
